import React from 'react';

const Button=()=>{
    return(
        <div>
            <button className="ui right labeled icon black button"><i class="right arrow icon"></i>Register</button>
        </div>
    );
}
export default Button;